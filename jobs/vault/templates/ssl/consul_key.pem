<%=
  key = ""
  if_p("vault.storage.consul.tls.key") do |consul_key|
    key = consul_key
  end
  if_link("consul_servers") do |consul|
    consul.if_p("consul.ssl_key") do |consul_key|
      key = consul_key
    end
  end
  key.strip
%>
