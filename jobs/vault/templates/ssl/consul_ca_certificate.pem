<%=
  cert = ""
  if_p("vault.storage.consul.tls.ca_certificate") do |ca_cert|
    cert = ca_cert
  end
  if_link("consul_servers") do |consul|
    consul.if_p("consul.ssl_ca") do |ca_cert|
      cert = ca_cert
    end
  end
  cert.strip
%>
