<%=
  certificate = ""
  if_p("vault.storage.consul.tls.certificate") do |consul_certificate|
    certificate = consul_certificate
  end
  if_link("consul_servers") do |consul|
    consul.if_p("consul.ssl_cert") do |consul_certificate|
    certificate = consul_certificate
    end
  end
  certificate.strip
%>
